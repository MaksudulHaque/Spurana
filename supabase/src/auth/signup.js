/* SPURANA · auth/signup.js (STEP 2) — Begin tab (sign up, consent-gated). */
(function () { "use strict"; Router.register("signup", function (root) { return window.renderAuth(root, "reg"); }); })();
